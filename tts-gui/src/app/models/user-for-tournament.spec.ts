import { UserForTournament } from './user-for-tournament';

describe('UserForTournament', () => {
  it('should create an instance', () => {
    expect(new UserForTournament()).toBeTruthy();
  });
});
